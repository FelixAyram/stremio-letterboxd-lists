# Render free: duerme tras 15 min sin tráfico. Ping cada 5 min.
name: Keep Render awake

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
  push:
    branches: [master]
    paths:
      - '.github/workflows/keepalive.yml'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake server + warm catalogs
        run: |
          BASE="https://stremio-letterboxd-lists.onrender.com"
          curl -fsS --max-time 120 "$BASE/api/health" -o /dev/null
          curl -fsS --max-time 180 "$BASE/api/wake" -o /dev/null
          curl -fsS --max-time 120 "$BASE/manifest.json" -o /dev/null
          echo "OK $(date -u +%Y-%m-%dT%H:%M:%SZ)"
